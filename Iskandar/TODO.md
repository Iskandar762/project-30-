# TODO
- [x] Create shared `css/styles.css` and `js/app.js`.
- [x] Updated `index.html` and `dashboard.html` to reference external `css/styles.css` and `js/app.js`.
- [ ] Extract remaining inline `<style>` blocks from all HTML pages into external CSS files.
- [ ] Extract remaining inline `<script>` blocks from all HTML pages into external JS files (shared or page-specific).

- [ ] Update each HTML file to reference new CSS/JS using `<link rel="stylesheet">` and `<script src="..." defer></script>`.

- [ ] Keep `modal-viewer.js` usage consistent (either reuse or move history-page modal logic into a JS module).
- [ ] Test key pages in browser: index, dashboard, smart-fuel-range-estimator, smart-fuel-range-history, navigation toggle.

